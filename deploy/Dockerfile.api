FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml .
RUN pip install uv && uv pip install --system -e ".[dev]"
COPY src/ src/
COPY config/ config/
EXPOSE 8000
CMD ["uvicorn", "reflexlearn.main:app", "--host", "0.0.0.0", "--port", "8000"]
