# P0 前端登录与生产安全加固 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 恢复 P0 鉴权后的前端可用性，并补齐生产环境安全开关：用户能在前端登录，chat / upload / video 请求都携带 Bearer token；生产环境禁止默认 demo 密码和关闭鉴权；脚本拆分安全冒烟与依赖真写。

**Architecture:** 后端只做 P0-1 的加固，不引入数据库用户表；新增 `validate_auth_runtime` 统一校验生产安全配置。前端采用短期 access token MVP：token 只放内存并同步到 `sessionStorage`，不进 `localStorage`，不存 refresh token；这是演示闭环，不是最终生产会话方案。请求侧新增统一 `authFetch` / `parseSSEStream` token 注入，避免每个组件手写 Authorization。

**Tech Stack:** Python 3.11+、FastAPI、Pydantic v2、Next.js 15.4、React 19、TypeScript、现有 `scripts/*.sh`。

---

## 0. 执行边界

本计划只做下一包，不做以下事项：

- 不做数据库用户体系。
- 不做注册、找回密码、组织邀请。
- 不做 HttpOnly Cookie + CSRF 的最终生产会话方案。
- 不做完整产品信息架构重构。
- 不做测试目录分层重构。
- 不做上传隔离区、病毒扫描、内容审核、防盗链签名 URL。
- 不做 AI Safety Gateway。

本包完成后，可以说：

- 后端生产环境不会允许默认 demo 密码或 `AUTH_ENABLED=false` 逃生口。
- 当前前端页面在 P0 Bearer 鉴权开启后恢复可用。
- 活体脚本能区分“安全冒烟通过”和“Qdrant/PG 真写依赖未开”。

不能说：

- 不能说已经有生产级用户体系。
- 不能说 token 存储方案已经达到最终生产安全级别。
- 不能说上传全链路审核、防盗链、对象隔离已经完成。

---

## 1. 执行前必须阅读

- `discuss/P0-最小安全闭环执行计划.md`
- `discuss/Claude-Code-Best对标审查与ReflexLearn优化路线.md`
- `src/reflexlearn/common/auth.py`
- `src/reflexlearn/api/deps.py`
- `src/reflexlearn/api/routes/auth.py`
- `tests/unit/test_auth_security.py`
- `frontend/app/page.tsx`
- `frontend/lib/sse.ts`
- `frontend/lib/useChat.ts`
- `frontend/components/KnowledgeUpload.tsx`
- `frontend/components/VideoJobCard.tsx`
- `frontend/lib/types.ts`
- `scripts/check_api.sh`
- `scripts/build_frontend.sh`
- `scripts/start_frontend.sh`

已知坏味道：

- `frontend/app/page.tsx` 目前承担整个首页工作台，后续会继续膨胀。本包只抽出 `components/Workspace.tsx` 和 `components/AuthGate.tsx`，不做完整路由重构。
- `frontend/components/KnowledgeUpload.tsx` 仍有 `any`。本包要顺手修掉它，因为涉及鉴权后的请求和响应类型。

---

## 2. 文件结构

### 新建文件

- `frontend/lib/auth.ts`  
  定义 `CurrentUser`、`AuthToken`、sessionStorage key、`login`、`getStoredAuth`、`storeAuth`、`clearAuth`。

- `frontend/lib/apiClient.ts`  
  定义 `authFetch`、`apiJson`、`apiForm`、`getErrorMessage`，统一注入 `Authorization`。

- `frontend/components/AuthGate.tsx`  
  最小登录门禁。登录成功后渲染工作台，支持退出。

- `frontend/components/Workspace.tsx`  
  从 `app/page.tsx` 抽出的原主工作台，接收 `token` 和 `user`。

- `scripts/check_api_security.sh`  
  只检查 health、login、me、未登录 401、非法上传 415、video 提交鉴权。无需 Qdrant/PG。

- `scripts/check_api_integrations.sh`  
  原 `check_api.sh` 的真写检查：上传必须写 Qdrant/PG，视频无 SeeDance 时 degraded。

### 修改文件

- `src/reflexlearn/common/auth.py`  
  新增 `validate_auth_runtime(settings)`，生产环境禁止默认 secret、默认 demo 密码、`AUTH_ENABLED=false`。

- `src/reflexlearn/api/deps.py`  
  `get_current_user` 调用 `validate_auth_runtime`；生产环境关闭 auth 时返回 503 或启动期失败，不允许直接 admin。

- `src/reflexlearn/api/routes/auth.py`  
  login 前调用 `validate_auth_runtime`。

- `src/reflexlearn/api/app.py`  
  `create_app()` 调用 `validate_auth_runtime(settings)`，让危险生产配置尽早失败。

- `tests/unit/test_auth_security.py`  
  补生产环境禁用 auth / 默认 demo 密码测试，补脚本拆分后安全冒烟契约相关单测可选。

- `frontend/app/page.tsx`  
  改为只渲染 `AuthGate`。

- `frontend/lib/sse.ts`  
  `parseSSEStream` 接收 `token`，请求头加 Authorization。

- `frontend/lib/useChat.ts`  
  `useChat(token: string)`，调用 `parseSSEStream(..., token)`；删除 `userId` 参数对后端身份的误导。

- `frontend/components/KnowledgeUpload.tsx`  
  接收 `token`，使用 `apiForm<IngestResult>`，去掉 `any`。

- `frontend/components/VideoJobCard.tsx`  
  接收 `token`，提交和轮询使用 `apiJson` / `authFetch`，去掉 `catch (e: any)`。

- `frontend/lib/types.ts`  
  将 `SSEMessage.data: any` 改为 `unknown`，新增类型守卫或安全读取 helper。

- `scripts/check_api.sh`  
  改为兼容入口：默认调用 `check_api_security.sh`，提示真写检查用 `check_api_integrations.sh`。

- `README.md` / `PROGRESS.md`  
  更新脚本说明和本包限制。

---

## 3. Task 1：后端生产安全开关加固

**Files:**

- Modify: `src/reflexlearn/common/auth.py`
- Modify: `src/reflexlearn/api/deps.py`
- Modify: `src/reflexlearn/api/routes/auth.py`
- Modify: `src/reflexlearn/api/app.py`
- Test: `tests/unit/test_auth_security.py`

- [ ] **Step 1: 写失败测试**

在 `tests/unit/test_auth_security.py` 追加：

```python
from reflexlearn.common.auth import validate_auth_runtime


def test_production_rejects_auth_disabled():
    settings = Settings(
        app_env="production",
        auth_enabled=False,
        auth_secret_key="x" * 48,
        auth_demo_password="changed-password",
    )
    with pytest.raises(AuthError):
        validate_auth_runtime(settings)


def test_production_rejects_default_demo_password():
    settings = Settings(
        app_env="production",
        auth_secret_key="x" * 48,
        auth_demo_password="reflexlearn-admin",
    )
    with pytest.raises(AuthError):
        validate_auth_runtime(settings)


def test_development_allows_demo_password_for_local_smoke():
    settings = Settings(app_env="development")
    validate_auth_runtime(settings)
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：失败，提示 `validate_auth_runtime` 不存在。

- [ ] **Step 3: 修改 `src/reflexlearn/common/auth.py`**

新增：

```python
def validate_auth_runtime(settings: Settings | None = None) -> None:
    cfg = _settings(settings)
    is_prod = cfg.app_env.lower() == "production"
    if not is_prod:
        return
    if not cfg.auth_enabled:
        raise AuthError("auth cannot be disabled in production")
    _assert_secret(cfg)
    if cfg.auth_demo_password == "reflexlearn-admin":
        raise AuthError("default demo password cannot be used in production")
```

注意：保留 `_assert_secret` 中的生产默认 secret 校验，不要删除现有 `test_production_rejects_default_auth_secret`。

- [ ] **Step 4: 修改 `src/reflexlearn/api/deps.py`**

新增 import：

```python
from reflexlearn.common.auth import AuthError, CurrentUser, validate_auth_runtime, verify_token
```

在 `get_current_user` 开头：

```python
    try:
        validate_auth_runtime(settings)
    except AuthError as exc:
        raise HTTPException(
            status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
            detail="auth_misconfigured",
        ) from exc
```

保留开发态 `AUTH_ENABLED=false` 返回 admin 的行为，但它必须在 `validate_auth_runtime` 之后。

- [ ] **Step 5: 修改 `src/reflexlearn/api/routes/auth.py`**

import 中加入 `validate_auth_runtime`，`login` 中 `settings = get_settings()` 后立刻：

```python
    try:
        validate_auth_runtime(settings)
    except AuthError as exc:
        raise HTTPException(
            status_code=status.HTTP_503_SERVICE_UNAVAILABLE,
            detail="auth_misconfigured",
        ) from exc
```

- [ ] **Step 6: 修改 `src/reflexlearn/api/app.py`**

新增 import：

```python
from reflexlearn.common.auth import validate_auth_runtime
```

在 `settings = get_settings()` 后加：

```python
    validate_auth_runtime(settings)
```

说明：这会让生产危险配置在 app 创建时失败。测试中如果需要 production 特殊设置，要清理 `get_settings` 缓存或直接测 `validate_auth_runtime`。

- [ ] **Step 7: 运行定向测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：全部通过。

---

## 4. Task 2：拆分 API 活体脚本

**Files:**

- Create: `scripts/check_api_security.sh`
- Create: `scripts/check_api_integrations.sh`
- Modify: `scripts/check_api.sh`

- [ ] **Step 1: 新建 `scripts/check_api_security.sh`**

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/_lib.sh"

ensure_logs
cd_root
use_local_network
use_python_defaults
export API_PORT="${1:-${API_PORT:-8000}}"

{
  log_header "check_api_security"
  "$(python_cmd)" - "$API_PORT" <<'PY'
import asyncio
import os
import sys
import time

import httpx

API = f"http://127.0.0.1:{sys.argv[1]}/api"
AUTH_USER = os.getenv("REFLEXLEARN_HEALTH_USER", "admin")
AUTH_PASSWORD = os.getenv("REFLEXLEARN_HEALTH_PASSWORD", "reflexlearn-admin")


async def wait_api(client: httpx.AsyncClient) -> None:
    deadline = time.time() + 60
    last_error = None
    while time.time() < deadline:
        try:
            resp = await client.get(f"{API}/health")
            if resp.status_code == 200 and resp.json().get("status") == "ok":
                print("[OK] api health")
                return
        except Exception as e:
            last_error = e
        await asyncio.sleep(1)
    raise RuntimeError(f"api health timeout: {last_error}")


async def login_headers(client: httpx.AsyncClient) -> dict[str, str]:
    resp = await client.post(
        f"{API}/auth/login",
        json={"username": AUTH_USER, "password": AUTH_PASSWORD},
    )
    resp.raise_for_status()
    token = resp.json()["access_token"]
    print("[OK] api auth login")
    return {"Authorization": f"Bearer {token}"}


async def check_me(client: httpx.AsyncClient, headers: dict[str, str]) -> None:
    resp = await client.get(f"{API}/auth/me", headers=headers)
    resp.raise_for_status()
    body = resp.json()
    if body.get("user_id") != AUTH_USER:
        raise RuntimeError(f"unexpected /me body: {body}")
    print("[OK] api auth me")


async def check_unauthorized(client: httpx.AsyncClient) -> None:
    chat = await client.post(f"{API}/chat", json={"message": "hi"})
    upload = await client.post(
        f"{API}/knowledge/upload",
        files={"file": ("note.txt", b"hello", "text/plain")},
    )
    video = await client.post(f"{API}/video/jobs", json={"storyboard": "scene"})
    if [chat.status_code, upload.status_code, video.status_code] != [401, 401, 401]:
        raise RuntimeError(
            f"expected 401 for protected routes, got "
            f"chat={chat.status_code} upload={upload.status_code} video={video.status_code}"
        )
    print("[OK] protected routes require bearer token")


async def check_upload_guard(client: httpx.AsyncClient, headers: dict[str, str]) -> None:
    resp = await client.post(
        f"{API}/knowledge/upload",
        files={"file": ("shell.exe", b"MZ\\x00\\x00", "application/octet-stream")},
        headers=headers,
    )
    if resp.status_code != 415:
        raise RuntimeError(f"expected upload guard 415, got {resp.status_code}: {resp.text}")
    print("[OK] upload guard rejects unsupported file")


async def check_video_auth(client: httpx.AsyncClient, headers: dict[str, str]) -> None:
    resp = await client.post(
        f"{API}/video/jobs",
        json={"storyboard": "Scene 1: Auth smoke."},
        headers=headers,
    )
    resp.raise_for_status()
    if not resp.json().get("job_id"):
        raise RuntimeError(f"missing job_id: {resp.text}")
    print("[OK] video submit with auth")


async def main() -> None:
    async with httpx.AsyncClient(timeout=30) as client:
        await wait_api(client)
        await check_unauthorized(client)
        headers = await login_headers(client)
        await check_me(client, headers)
        await check_upload_guard(client, headers)
        await check_video_auth(client, headers)


asyncio.run(main())
PY
} 2>&1 | tee -a "$LOG_DIR/check_api_security.log"
```

- [ ] **Step 2: 新建 `scripts/check_api_integrations.sh`**

从当前 `scripts/check_api.sh` 复制完整真写检查逻辑，保留：

- health
- login
- 上传必须 `chunks >= 1`
- 上传必须 `qdrant_written >= 1`
- 上传必须 `pg_written is True`
- 视频无 SeeDance key 时必须 degraded

日志文件改为：

```bash
} 2>&1 | tee -a "$LOG_DIR/check_api_integrations.log"
```

- [ ] **Step 3: 修改 `scripts/check_api.sh`**

改成兼容入口：

```bash
#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "[INFO] check_api now runs security smoke only. Use scripts/check_api_integrations.sh for Qdrant/PG true-write checks."
exec "$SCRIPT_DIR/check_api_security.sh" "$@"
```

- [ ] **Step 4: 校验 shell 语法**

运行：

```bash
bash -n scripts/*.sh
```

预期：无输出，退出码 0。

---

## 5. Task 3：前端认证类型与 API 客户端

**Files:**

- Create: `frontend/lib/auth.ts`
- Create: `frontend/lib/apiClient.ts`
- Modify: `frontend/lib/types.ts`

- [ ] **Step 1: 新建 `frontend/lib/auth.ts`**

内容：

```typescript
import type { CurrentUser, AuthToken } from "./types";

const AUTH_KEY = "reflexlearn_auth";

const API_BASE = process.env.NEXT_PUBLIC_API_BASE || "http://localhost:8000/api";

function isCurrentUser(value: unknown): value is CurrentUser {
  if (!value || typeof value !== "object") return false;
  const v = value as Record<string, unknown>;
  return (
    typeof v.user_id === "string" &&
    typeof v.tenant_id === "string" &&
    typeof v.role === "string"
  );
}

function isAuthToken(value: unknown): value is AuthToken {
  if (!value || typeof value !== "object") return false;
  const v = value as Record<string, unknown>;
  return (
    typeof v.access_token === "string" &&
    typeof v.token_type === "string" &&
    typeof v.expires_in === "number" &&
    isCurrentUser(v.user)
  );
}

export function getStoredAuth(): AuthToken | null {
  if (typeof window === "undefined") return null;
  const raw = window.sessionStorage.getItem(AUTH_KEY);
  if (!raw) return null;
  try {
    const parsed: unknown = JSON.parse(raw);
    return isAuthToken(parsed) ? parsed : null;
  } catch {
    return null;
  }
}

export function storeAuth(auth: AuthToken): void {
  if (typeof window === "undefined") return;
  window.sessionStorage.setItem(AUTH_KEY, JSON.stringify(auth));
}

export function clearAuth(): void {
  if (typeof window === "undefined") return;
  window.sessionStorage.removeItem(AUTH_KEY);
}

export async function login(username: string, password: string): Promise<AuthToken> {
  const resp = await fetch(`${API_BASE}/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ username, password }),
  });
  if (!resp.ok) {
    throw new Error(resp.status === 401 ? "账号或密码错误" : `登录失败：HTTP ${resp.status}`);
  }
  const body: unknown = await resp.json();
  if (!isAuthToken(body)) throw new Error("登录响应格式不正确");
  storeAuth(body);
  return body;
}
```

注意：这里使用 `sessionStorage`，不是 `localStorage`。不要存 refresh token，不要把 token 打到 console。

- [ ] **Step 2: 新建 `frontend/lib/apiClient.ts`**

内容：

```typescript
export function getErrorMessage(e: unknown): string {
  return e instanceof Error ? e.message : "请求失败";
}

export async function authFetch(
  url: string,
  token: string,
  init: RequestInit = {},
): Promise<Response> {
  const headers = new Headers(init.headers);
  headers.set("Authorization", `Bearer ${token}`);
  return fetch(url, { ...init, headers });
}

export async function apiJson<T>(
  url: string,
  token: string,
  init: RequestInit = {},
): Promise<T> {
  const headers = new Headers(init.headers);
  headers.set("Content-Type", "application/json");
  const resp = await authFetch(url, token, { ...init, headers });
  if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
  return (await resp.json()) as T;
}

export async function apiForm<T>(
  url: string,
  token: string,
  body: FormData,
): Promise<T> {
  const resp = await authFetch(url, token, { method: "POST", body });
  if (!resp.ok) throw new Error(`HTTP ${resp.status}`);
  return (await resp.json()) as T;
}
```

- [ ] **Step 3: 修改 `frontend/lib/types.ts`**

新增：

```typescript
export type UserRole = "student" | "teacher" | "admin" | "evaluator";

export interface CurrentUser {
  user_id: string;
  tenant_id: string;
  role: UserRole;
}

export interface AuthToken {
  access_token: string;
  token_type: string;
  expires_in: number;
  user: CurrentUser;
}
```

将：

```typescript
export interface SSEMessage {
  event: string;
  data: any;
}
```

改为：

```typescript
export interface SSEMessage {
  event: string;
  data: unknown;
}
```

- [ ] **Step 4: 构建验证失败点**

运行：

```bash
bash scripts/build_frontend.sh
```

预期：当前可能失败，因为 `useChat.ts` 还按 `msg.data?.session_id` 访问 unknown。下一任务修。

---

## 6. Task 4：SSE 和聊天请求注入 token

**Files:**

- Modify: `frontend/lib/sse.ts`
- Modify: `frontend/lib/useChat.ts`

- [ ] **Step 1: 修改 `frontend/lib/sse.ts`**

函数签名改为：

```typescript
export async function* parseSSEStream(
  url: string,
  body: unknown,
  signal: AbortSignal,
  token: string,
): AsyncGenerator<SSEMessage> {
```

headers 改为：

```typescript
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${token}`,
    },
```

- [ ] **Step 2: 修改 `frontend/lib/useChat.ts`**

新增安全读取 helper：

```typescript
function asRecord(value: unknown): Record<string, unknown> {
  return value && typeof value === "object" ? (value as Record<string, unknown>) : {};
}

function asString(value: unknown): string {
  return typeof value === "string" ? value : "";
}
```

签名改为：

```typescript
export function useChat(token: string) {
```

`send` 签名改为：

```typescript
  const send = useCallback(async (message: string) => {
```

调用 `parseSSEStream` 改为：

```typescript
      for await (const msg of parseSSEStream(
        `${base}/chat`,
        { message, session_id: sessionRef.current },
        ctrl.signal,
        token,
      )) {
```

事件处理里替换所有直接 `msg.data?.xxx`：

```typescript
          case "session": {
            const data = asRecord(msg.data);
            sessionRef.current = asString(data.session_id) || sessionRef.current;
            if (typeof window !== "undefined" && sessionRef.current) {
              localStorage.setItem(SID_KEY, sessionRef.current);
            }
            break;
          }
          case "agent_step":
            dispatch({ type: "agent_step", payload: msg.data as AgentStep });
            break;
          case "resource_card":
            dispatch({ type: "resource_card", payload: msg.data as ResourceCard });
            break;
          case "debate_round":
            dispatch({ type: "debate_round", payload: msg.data as DebateRound });
            break;
          case "judge_verdict":
            dispatch({ type: "judge_verdict", payload: msg.data as JudgeVerdict });
            break;
          case "learning_path":
            dispatch({ type: "learning_path", payload: msg.data as LearningPath });
            break;
          case "error": {
            const data = asRecord(msg.data);
            errored = true;
            dispatch({ type: "error", payload: asString(data.error) || "服务端出错" });
            break;
          }
```

`useCallback` 依赖改为 `[token]`。

- [ ] **Step 3: 构建验证**

运行：

```bash
bash scripts/build_frontend.sh
```

预期：可能仍失败，因为页面还没传 token。下一任务修。

---

## 7. Task 5：前端登录门禁和工作台拆分

**Files:**

- Create: `frontend/components/AuthGate.tsx`
- Create: `frontend/components/Workspace.tsx`
- Modify: `frontend/app/page.tsx`

- [ ] **Step 1: 新建 `frontend/components/Workspace.tsx`**

把 `frontend/app/page.tsx` 原有主内容搬到这里，文件开头：

```typescript
"use client";

import type { CurrentUser } from "@/lib/types";
import { useChat } from "@/lib/useChat";
import { ChatInput } from "@/components/ChatInput";
import { AgentTimeline } from "@/components/AgentTimeline";
import { ResourceCard } from "@/components/ResourceCard";
import { DebatePanel } from "@/components/DebatePanel";
import { LearningPathCard } from "@/components/LearningPathCard";
import { KnowledgeUpload } from "@/components/KnowledgeUpload";
import { VideoJobCard } from "@/components/VideoJobCard";

interface WorkspaceProps {
  token: string;
  user: CurrentUser;
  onLogout: () => void;
}

export function Workspace({ token, user, onLogout }: WorkspaceProps) {
  const { turns, send, stop, resetSession, streaming } = useChat(token);
```

header 右侧改为同时显示用户和退出：

```tsx
        <div className="flex shrink-0 items-center gap-2">
          <div className="hidden text-right text-xs text-slate-500 sm:block">
            <div className="font-medium text-slate-700">{user.user_id}</div>
            <div>{user.tenant_id} · {user.role}</div>
          </div>
          {turns.length > 0 && (
            <button
              onClick={resetSession}
              className="rounded-lg border border-slate-300 px-3 py-1.5 text-xs font-medium text-slate-600 transition hover:border-indigo-400 hover:text-indigo-600"
            >
              新会话
            </button>
          )}
          <button
            onClick={onLogout}
            className="rounded-lg border border-slate-300 px-3 py-1.5 text-xs font-medium text-slate-600 transition hover:border-rose-400 hover:text-rose-600"
          >
            退出
          </button>
        </div>
```

工具区传 token：

```tsx
          <KnowledgeUpload token={token} />
          <VideoJobCard token={token} />
```

其余 JSX 保持原样。

- [ ] **Step 2: 新建 `frontend/components/AuthGate.tsx`**

内容：

```typescript
"use client";

import { FormEvent, useEffect, useState } from "react";
import { clearAuth, getStoredAuth, login } from "@/lib/auth";
import type { AuthToken } from "@/lib/types";
import { getErrorMessage } from "@/lib/apiClient";
import { Workspace } from "@/components/Workspace";

type LoginStatus = "checking" | "idle" | "submitting" | "error";

export function AuthGate() {
  const [auth, setAuth] = useState<AuthToken | null>(null);
  const [status, setStatus] = useState<LoginStatus>("checking");
  const [username, setUsername] = useState("admin");
  const [password, setPassword] = useState("reflexlearn-admin");
  const [error, setError] = useState("");

  useEffect(() => {
    const stored = getStoredAuth();
    setAuth(stored);
    setStatus("idle");
  }, []);

  async function onSubmit(e: FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setStatus("submitting");
    setError("");
    try {
      const next = await login(username, password);
      setAuth(next);
      setStatus("idle");
    } catch (err: unknown) {
      setError(getErrorMessage(err));
      setStatus("error");
    }
  }

  function logout() {
    clearAuth();
    setAuth(null);
    setStatus("idle");
  }

  if (auth) {
    return <Workspace token={auth.access_token} user={auth.user} onLogout={logout} />;
  }

  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 px-4">
      <section className="w-full max-w-sm rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
        <div className="mb-5">
          <h1 className="text-xl font-semibold text-slate-900">ReflexLearn</h1>
          <p className="mt-1 text-sm text-slate-500">登录后进入多智能体学习工作台</p>
        </div>
        <form onSubmit={onSubmit} className="space-y-3">
          <label className="block text-sm font-medium text-slate-700">
            账号
            <input
              value={username}
              onChange={(e) => setUsername(e.target.value)}
              className="mt-1 w-full rounded-md border border-slate-300 px-3 py-2 text-sm"
              autoComplete="username"
            />
          </label>
          <label className="block text-sm font-medium text-slate-700">
            密码
            <input
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              type="password"
              className="mt-1 w-full rounded-md border border-slate-300 px-3 py-2 text-sm"
              autoComplete="current-password"
            />
          </label>
          {status === "error" && (
            <div className="rounded-md border border-rose-200 bg-rose-50 px-3 py-2 text-sm text-rose-700">
              {error}
            </div>
          )}
          <button
            type="submit"
            disabled={status === "submitting"}
            className="w-full rounded-md bg-indigo-600 px-3 py-2 text-sm font-medium text-white transition hover:bg-indigo-700 disabled:opacity-50"
          >
            {status === "submitting" ? "登录中..." : "登录"}
          </button>
        </form>
      </section>
    </main>
  );
}
```

注意：这个登录页是产品工作台门禁，不是营销页。不要加大 hero，不要加装饰性背景。

- [ ] **Step 3: 修改 `frontend/app/page.tsx`**

替换为：

```typescript
import { AuthGate } from "@/components/AuthGate";

export default function Home() {
  return <AuthGate />;
}
```

- [ ] **Step 4: 构建验证**

运行：

```bash
bash scripts/build_frontend.sh
```

预期：可能仍失败，因为 KnowledgeUpload / VideoJobCard prop 未改。下一任务修。

---

## 8. Task 6：上传和视频组件注入 token，去掉 any

**Files:**

- Modify: `frontend/components/KnowledgeUpload.tsx`
- Modify: `frontend/components/VideoJobCard.tsx`

- [ ] **Step 1: 修改 `KnowledgeUpload` props 和类型**

import 增加：

```typescript
import type { IngestResult } from "@/lib/types";
import { apiForm, getErrorMessage } from "@/lib/apiClient";
```

增加 props：

```typescript
interface KnowledgeUploadProps {
  token: string;
}
```

函数签名：

```typescript
export function KnowledgeUpload({ token }: KnowledgeUploadProps) {
```

state 改为：

```typescript
  const [result, setResult] = useState<IngestResult | null>(null);
```

请求改为：

```typescript
      const data = await apiForm<IngestResult>(`${API_BASE}/knowledge/upload`, token, fd);
      setResult(data);
```

catch 改为：

```typescript
    } catch (e: unknown) {
      setError(getErrorMessage(e));
      setStatus("error");
    }
```

`IngestResultPanel` 签名改为：

```typescript
function IngestResultPanel({ result }: { result: IngestResult }) {
```

- [ ] **Step 2: 修改 `VideoJobCard` props 和请求**

import 增加：

```typescript
import { apiJson, authFetch, getErrorMessage } from "@/lib/apiClient";
```

新增 props：

```typescript
interface VideoJobCardProps {
  token: string;
}
```

函数签名：

```typescript
export function VideoJobCard({ token }: VideoJobCardProps) {
```

轮询请求改为：

```typescript
        const r = await authFetch(`${API_BASE}/video/jobs/${jobId}`, token);
        if (!r.ok) throw new Error(`HTTP ${r.status}`);
        const j: VideoJob = await r.json();
```

提交请求改为：

```typescript
      const data = await apiJson<{ job_id: string; status: string }>(
        `${API_BASE}/video/jobs`,
        token,
        {
          method: "POST",
          body: JSON.stringify({ storyboard }),
        },
      );
```

两个 catch 都改为 `catch (e: unknown)` 并使用 `getErrorMessage(e)`。

`poll` 的 `useCallback` 依赖改为 `[token]`。

`submit` 依赖保持 `[storyboard, poll, token]` 或只 `[storyboard, poll]`，取决于 token 是否只在 poll 里使用；如果 submit 直接使用 token，必须包含 token。

- [ ] **Step 3: 搜索确认没有 any**

运行：

```bash
rg -n "any|catch \\(e: any\\)|result: any|SSEMessage.*any" frontend/app frontend/components frontend/lib
```

预期：不再出现 `KnowledgeUpload`、`VideoJobCard`、`SSEMessage` 相关 any。`MarkdownView` 的第三方 style cast 如仍存在，可留到后续单独处理。

- [ ] **Step 4: 构建验证**

运行：

```bash
bash scripts/build_frontend.sh
```

预期：构建通过。

---

## 9. Task 7：联调安全冒烟

**Files:**

- Potentially modify files touched above

- [ ] **Step 1: 运行后端定向测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：通过。

- [ ] **Step 2: 运行全量单测**

运行：

```bash
bash scripts/test_unit.sh
```

预期：通过。当前基线是 `309 passed, 2 warnings`，本包后测试数量会增加。

- [ ] **Step 3: 运行脚本语法检查**

运行：

```bash
bash -n scripts/*.sh
```

预期：无输出，退出码 0。

- [ ] **Step 4: 构建前端**

运行：

```bash
bash scripts/build_frontend.sh
```

预期：Next build 通过。

- [ ] **Step 5: 可选活体安全冒烟**

如果当前机器允许启动后端：

```bash
bash scripts/start_api.sh 8002
bash scripts/check_api_security.sh 8002
bash scripts/stop_api.sh 8002
```

预期：

- `[OK] api health`
- `[OK] protected routes require bearer token`
- `[OK] api auth login`
- `[OK] api auth me`
- `[OK] upload guard rejects unsupported file`
- `[OK] video submit with auth`

如果还要验证 Qdrant/PG 真写，必须先启动中间件：

```bash
bash scripts/start_bigdata.sh
bash scripts/check_api_integrations.sh 8002
```

这一步失败不能归咎为 P0 前端/鉴权失败，除非安全冒烟也失败。

---

## 10. Task 8：更新文档

**Files:**

- Modify: `README.md`
- Modify: `PROGRESS.md`

- [ ] **Step 1: 更新 README 脚本说明**

在 API 检查部分加入：

```markdown
P0 安全底座后：

```bash
bash scripts/check_api_security.sh 8001       # 只检查鉴权、受保护路由、上传 guard、视频提交
bash scripts/check_api_integrations.sh 8001   # 检查 Qdrant/PG 真写和视频降级，需要依赖中间件可用
```

`scripts/check_api.sh` 现在兼容调用安全冒烟；依赖真写请显式运行 `check_api_integrations.sh`。
```

- [ ] **Step 2: 更新 PROGRESS 顶部与本轮记录**

追加：

```markdown
P0 第二包完成：后端生产环境安全开关加固，禁止生产关闭 auth 和默认 demo 密码；前端新增最小登录门禁，chat / knowledge upload / video job 请求统一携带 Bearer token；脚本拆分为安全冒烟与依赖真写两类检查；前端构建通过。
```

限制说明追加：

```markdown
诚实限制：当前前端 token 方案为短期 Bearer MVP，使用 sessionStorage 恢复刷新态，不是最终生产会话方案。后续应升级为 HttpOnly Cookie + CSRF、数据库用户体系、登录限流、审计和对象级资源归属。
```

---

## 11. 提交前自查清单

- [ ] `APP_ENV=production` 且 `AUTH_ENABLED=false` 会失败。
- [ ] `APP_ENV=production` 且默认 demo 密码会失败。
- [ ] 前端 token 不进 `localStorage`。
- [ ] 前端没有 refresh token。
- [ ] chat / upload / video 请求都带 `Authorization`。
- [ ] 未登录访问前端只看到登录门禁。
- [ ] 退出会清理 sessionStorage auth。
- [ ] `frontend/lib/types.ts` 不再有 `SSEMessage.data: any`。
- [ ] `KnowledgeUpload.tsx` 不再有 `result: any` 和 `catch (e: any)`。
- [ ] `VideoJobCard.tsx` 不再有 `catch (e: any)`。
- [ ] `scripts/check_api_security.sh` 不依赖 Qdrant/PG。
- [ ] `scripts/check_api_integrations.sh` 保留 Qdrant/PG 真写检查。
- [ ] 所有 Run & Debug 仍通过 `scripts/*.sh`。

---

## 12. 回报模板

新窗口执行完成后，按以下格式回报：

```markdown
## P0 前端登录与生产安全加固执行回报

### 1. 完成范围
- 完成 Task：
- 未完成 Task：

### 2. 修改文件
- 新建：
- 修改：

### 3. 关键实现
- 后端生产安全校验：
- 前端 token 存储策略：
- Authorization 注入点：
- 脚本拆分：

### 4. 验证结果
- `bash scripts/test_unit.sh tests/unit/test_auth_security.py -q`：
- `bash scripts/test_unit.sh`：
- `bash -n scripts/*.sh`：
- `bash scripts/build_frontend.sh`：
- `bash scripts/check_api_security.sh <port>`：
- `bash scripts/check_api_integrations.sh <port>`：

### 5. 已知限制
-

### 6. 需要 review 的点
-
```

---

## 13. Review 重点

主窗口后续 review 时优先检查：

1. 生产危险配置是否真的会失败。
2. token 是否进了 `localStorage` 或 console。
3. 是否有未带 Authorization 的受保护请求。
4. `any` 是否又扩散到新的前端核心类型。
5. `check_api_security.sh` 是否完全不依赖 Qdrant/PG。
6. 前端是否能在登录后恢复 chat / upload / video 的基础交互。
