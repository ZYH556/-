# P0 最小安全闭环 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 ReflexLearn 补上第一层可执行安全边界：登录拿 token、受保护 API 必须鉴权、租户身份由服务端注入、CORS 不再 `* + credentials`、上传有最小大小和类型校验。

**Architecture:** 这一阶段不做完整 SaaS 用户系统，不引入数据库用户表，不做前端登录页。先实现后端 `auth` 模块、FastAPI 依赖、公开 `/api/auth/login` 与 `/api/auth/me`，并保护 chat / knowledge / video 三组接口。上传链路新增 `upload_guard` 作为唯一读取文件入口，避免 route 直接 `file.read()`。

**Tech Stack:** Python 3.11+、FastAPI、Pydantic v2、标准库 `hmac/hashlib/base64/json/secrets/time`、现有 `scripts/*.sh` 测试和活体脚本。

---

## 0. 执行边界

本计划只做 P0 第一包，不做以下事项：

- 不做 Next.js 登录页。
- 不做用户注册、找回密码、组织邀请。
- 不做数据库迁移和真实用户表。
- 不做病毒扫描、内容审核、防盗链签名 URL 的完整实现。
- 不做 AI Safety Gateway 的完整输入输出审核。
- 不改 Agent 编排质量逻辑。
- 不直接运行 `python`、`pytest`、`npm`、`pnpm`；所有测试和活体走 `scripts/*.sh`。

本计划完成后，项目仍不能宣称“生产级安全完备”，只能宣称“已具备后端最小鉴权、租户注入、CORS 收紧和上传基础校验”。

---

## 1. 执行前必须阅读

- `AGENTS.md` 或当前提示中的项目规则。
- `PROGRESS.md` 第 1、2、5、6 节。
- `discuss/Claude-Code-Best对标审查与ReflexLearn优化路线.md`。
- `src/reflexlearn/api/app.py`
- `src/reflexlearn/api/routes/chat.py`
- `src/reflexlearn/api/routes/knowledge.py`
- `src/reflexlearn/api/routes/video.py`
- `src/reflexlearn/common/config.py`
- `tests/unit/test_knowledge_upload.py`
- `tests/unit/test_kafka_io.py`
- `tests/unit/test_video_jobs.py`
- `scripts/check_api.sh`

已知坏味道：

- `tests/unit/` 文件数量已经远超项目“每层文件尽量不超过 8 个”的理想指标。这是历史结构问题，本计划不做大规模测试目录重构，避免扩大范围。执行者只新增 1 个安全测试文件，并在回报里标注这个坏味道仍然存在，后续可单独整理。

---

## 2. 文件结构

### 新建文件

- `src/reflexlearn/common/auth.py`  
  负责签发和验证服务端 HMAC token、演示用户认证、`CurrentUser` / `AuthToken` / `TokenPayload` 类型。

- `src/reflexlearn/api/deps.py`  
  负责 FastAPI 鉴权依赖：`get_current_user`、`require_roles`。

- `src/reflexlearn/api/routes/auth.py`  
  负责 `/api/auth/login` 和 `/api/auth/me`。

- `src/reflexlearn/api/upload_guard.py`  
  负责上传大小限制、扩展名/MIME/魔数校验、visibility 校验。

- `tests/unit/test_auth_security.py`  
  覆盖登录、token、受保护接口 401、租户伪造无效、上传拒绝非法文件、CORS 白名单。

### 修改文件

- `src/reflexlearn/common/config.py`  
  增加 auth、CORS、trusted host、upload 限制配置。

- `src/reflexlearn/api/app.py`  
  接入 auth router、CORS 白名单、TrustedHostMiddleware。

- `src/reflexlearn/api/routes/chat.py`  
  `/api/chat` 改为必须登录，`user_id` 使用 token 中的可信用户。

- `src/reflexlearn/api/routes/knowledge.py`  
  `/api/knowledge/upload` 改为必须登录，`user_id` / `tenant_id` 使用 token 中的可信上下文，上传读取走 `upload_guard`。

- `src/reflexlearn/api/routes/video.py`  
  `/api/video/jobs` 提交和查询都必须登录。

- `.env.example`  
  增加 auth、CORS、trusted host、upload 配置示例，不写真实密钥。

- `scripts/check_api.sh`  
  health 保持公开；上传和视频检查前先登录，后续请求携带 `Authorization: Bearer <token>`。

- `tests/unit/test_knowledge_upload.py`  
  route 测试加登录 header，并断言表单伪造 `user_id` / `tenant_id` 不再生效。

- `tests/unit/test_kafka_io.py`  
  route 测试加登录 header。

- `tests/unit/test_video_jobs.py`  
  route 测试加登录 header；补一个未登录 401 测试可放到 `test_auth_security.py`。

- `PROGRESS.md`  
  执行完成后追加本轮记录，更新 P0 状态和验证结果。

---

## 3. 设计细节

### 3.1 Token 方案

本阶段不引入 `PyJWT`，避免依赖升级和锁文件变更。实现一个服务端签名 token：

```text
base64url(header_json).base64url(payload_json).base64url(hmac_sha256(header.payload, AUTH_SECRET_KEY))
```

必须满足：

- token payload 强类型解析为 `TokenPayload`。
- 验签使用 `hmac.compare_digest`。
- 校验 `exp`、`iss`、`aud`。
- 生产环境 `APP_ENV=production` 时，如果 `AUTH_SECRET_KEY` 仍是默认值，启动或签发 token 必须失败。
- token 不存敏感信息，只存 `sub`、`tenant_id`、`role`、`iat`、`exp`、`iss`、`aud`、`jti`。

### 3.2 演示用户

本阶段只支持环境变量配置的演示用户：

- `AUTH_DEMO_USERNAME=admin`
- `AUTH_DEMO_PASSWORD=reflexlearn-admin`
- `AUTH_DEMO_TENANT_ID=default`
- `AUTH_DEMO_ROLE=admin`

生产系统后续必须替换为数据库用户表、密码哈希和会话刷新机制。不要在本阶段扩展注册/用户管理。

### 3.3 Auth 开关

`AUTH_ENABLED` 默认必须是 `true`。如果为了某些离线评测要关闭，只能显式设置 `AUTH_ENABLED=false`，并且执行者需要在回报中说明。

### 3.4 路由保护规则

| 路由 | 规则 |
|---|---|
| `GET /api/health` | 公开 |
| `POST /api/auth/login` | 公开 |
| `GET /api/auth/me` | 需要登录 |
| `POST /api/chat` | 需要登录，忽略请求体中的 `user_id` |
| `POST /api/knowledge/upload` | 需要登录，忽略表单中的 `user_id` / `tenant_id` |
| `POST /api/video/jobs` | 需要登录 |
| `GET /api/video/jobs/{job_id}` | 需要登录 |

---

## 4. Task 1：补配置项

**Files:**

- Modify: `src/reflexlearn/common/config.py`
- Modify: `.env.example`

- [ ] **Step 1: 写失败测试，证明配置项存在**

在 `tests/unit/test_auth_security.py` 新建以下测试骨架：

```python
from reflexlearn.common.config import Settings


def test_security_settings_defaults_are_explicit():
    settings = Settings()
    assert settings.auth_enabled is True
    assert settings.auth_token_ttl_seconds > 0
    assert settings.auth_issuer == "reflexlearn"
    assert "localhost" in settings.cors_allow_origins
    assert settings.max_upload_bytes > 0
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：失败，提示 `Settings` 没有 `auth_enabled` 等字段。

- [ ] **Step 3: 修改 `src/reflexlearn/common/config.py`**

在 `Settings` 中追加：

```python
    # —— P0 安全底座：认证 / CORS / 上传限制 ——
    app_env: str = "development"
    auth_enabled: bool = True
    auth_secret_key: str = "dev-only-change-me-reflexlearn-auth-secret"
    auth_token_ttl_seconds: int = 7200
    auth_issuer: str = "reflexlearn"
    auth_audience: str = "reflexlearn-web"
    auth_demo_username: str = "admin"
    auth_demo_password: str = "reflexlearn-admin"
    auth_demo_tenant_id: str = "default"
    auth_demo_role: str = "admin"

    cors_allow_origins: str = (
        "http://localhost:3000,http://localhost:3001,"
        "http://127.0.0.1:3000,http://127.0.0.1:3001"
    )
    trusted_hosts: str = "127.0.0.1,localhost,testserver"

    max_upload_bytes: int = 10 * 1024 * 1024
    allowed_upload_extensions: str = "pdf,docx,pptx,html,htm,md,txt"
    allowed_upload_mime_types: str = (
        "application/pdf,"
        "application/vnd.openxmlformats-officedocument.wordprocessingml.document,"
        "application/vnd.openxmlformats-officedocument.presentationml.presentation,"
        "text/html,text/markdown,text/plain"
    )
```

- [ ] **Step 4: 修改 `.env.example`**

追加：

```env
# === P0 安全底座 ===
APP_ENV=development
AUTH_ENABLED=true
AUTH_SECRET_KEY=change-me-to-a-long-random-secret
AUTH_TOKEN_TTL_SECONDS=7200
AUTH_ISSUER=reflexlearn
AUTH_AUDIENCE=reflexlearn-web
AUTH_DEMO_USERNAME=admin
AUTH_DEMO_PASSWORD=reflexlearn-admin
AUTH_DEMO_TENANT_ID=default
AUTH_DEMO_ROLE=admin

CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001
TRUSTED_HOSTS=127.0.0.1,localhost,testserver

MAX_UPLOAD_BYTES=10485760
ALLOWED_UPLOAD_EXTENSIONS=pdf,docx,pptx,html,htm,md,txt
ALLOWED_UPLOAD_MIME_TYPES=application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,application/vnd.openxmlformats-officedocument.presentationml.presentation,text/html,text/markdown,text/plain
```

- [ ] **Step 5: 运行测试验证通过**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：`test_security_settings_defaults_are_explicit` 通过。

---

## 5. Task 2：实现 token 与演示用户认证

**Files:**

- Create: `src/reflexlearn/common/auth.py`
- Test: `tests/unit/test_auth_security.py`

- [ ] **Step 1: 追加失败测试**

在 `tests/unit/test_auth_security.py` 追加：

```python
import time

import pytest

from reflexlearn.common.auth import (
    AuthError,
    CurrentUser,
    authenticate_demo_user,
    issue_token,
    verify_token,
)
from reflexlearn.common.config import Settings


def test_demo_user_login_and_token_roundtrip():
    settings = Settings()
    user = authenticate_demo_user("admin", "reflexlearn-admin", settings)
    assert user.user_id == "admin"
    assert user.tenant_id == "default"
    assert user.role == "admin"

    token = issue_token(user, settings)
    verified = verify_token(token, settings)
    assert verified == user


def test_wrong_password_rejected():
    settings = Settings()
    with pytest.raises(AuthError):
        authenticate_demo_user("admin", "bad-password", settings)


def test_tampered_token_rejected():
    settings = Settings()
    token = issue_token(CurrentUser(user_id="u1", tenant_id="t1", role="student"), settings)
    bad = token[:-2] + "xx"
    with pytest.raises(AuthError):
        verify_token(bad, settings)


def test_expired_token_rejected():
    settings = Settings(auth_token_ttl_seconds=-1)
    token = issue_token(CurrentUser(user_id="u1", tenant_id="t1", role="student"), settings)
    time.sleep(0.01)
    with pytest.raises(AuthError):
        verify_token(token, settings)


def test_production_rejects_default_auth_secret():
    settings = Settings(app_env="production")
    user = CurrentUser(user_id="u1", tenant_id="t1", role="student")
    with pytest.raises(AuthError):
        issue_token(user, settings)
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：失败，提示 `reflexlearn.common.auth` 不存在。

- [ ] **Step 3: 新建 `src/reflexlearn/common/auth.py`**

实现：

```python
from __future__ import annotations

import base64
import hashlib
import hmac
import json
import secrets
import time
from collections.abc import Mapping
from typing import Literal, cast

from pydantic import BaseModel, ValidationError

from reflexlearn.common.config import Settings, get_settings

Role = Literal["student", "teacher", "admin", "evaluator"]


class AuthError(Exception):
    """认证失败，API 层统一转换为 401。"""


class CurrentUser(BaseModel):
    user_id: str
    tenant_id: str
    role: Role = "student"


class TokenPayload(BaseModel):
    sub: str
    tenant_id: str
    role: Role
    iat: int
    exp: int
    iss: str
    aud: str
    jti: str


class AuthToken(BaseModel):
    access_token: str
    token_type: str = "bearer"
    expires_in: int
    user: CurrentUser


def _b64e(raw: bytes) -> str:
    return base64.urlsafe_b64encode(raw).rstrip(b"=").decode("ascii")


def _b64d(value: str) -> bytes:
    padding = "=" * (-len(value) % 4)
    return base64.urlsafe_b64decode((value + padding).encode("ascii"))


def _json_dumps(data: Mapping[str, object]) -> bytes:
    return json.dumps(data, ensure_ascii=False, separators=(",", ":"), sort_keys=True).encode("utf-8")


def _settings(settings: Settings | None) -> Settings:
    return settings or get_settings()


def _assert_secret(settings: Settings) -> None:
    default = "dev-only-change-me-reflexlearn-auth-secret"
    if settings.app_env.lower() == "production" and settings.auth_secret_key == default:
        raise AuthError("auth secret must be changed in production")
    if len(settings.auth_secret_key) < 32:
        raise AuthError("auth secret is too short")


def _role(value: str) -> Role:
    if value in {"student", "teacher", "admin", "evaluator"}:
        return cast(Role, value)
    raise AuthError("invalid configured role")


def authenticate_demo_user(username: str, password: str, settings: Settings | None = None) -> CurrentUser:
    cfg = _settings(settings)
    user_ok = hmac.compare_digest(username, cfg.auth_demo_username)
    pass_ok = hmac.compare_digest(password, cfg.auth_demo_password)
    if not (user_ok and pass_ok):
        raise AuthError("invalid credentials")
    return CurrentUser(
        user_id=cfg.auth_demo_username,
        tenant_id=cfg.auth_demo_tenant_id,
        role=_role(cfg.auth_demo_role),
    )


def issue_token(user: CurrentUser, settings: Settings | None = None) -> str:
    cfg = _settings(settings)
    _assert_secret(cfg)
    now = int(time.time())
    payload = TokenPayload(
        sub=user.user_id,
        tenant_id=user.tenant_id,
        role=user.role,
        iat=now,
        exp=now + cfg.auth_token_ttl_seconds,
        iss=cfg.auth_issuer,
        aud=cfg.auth_audience,
        jti=secrets.token_urlsafe(12),
    )
    header = {"alg": "HS256", "typ": "RLT"}
    signing_input = f"{_b64e(_json_dumps(header))}.{_b64e(payload.model_dump_json().encode('utf-8'))}"
    sig = hmac.new(
        cfg.auth_secret_key.encode("utf-8"),
        signing_input.encode("ascii"),
        hashlib.sha256,
    ).digest()
    return f"{signing_input}.{_b64e(sig)}"


def verify_token(token: str, settings: Settings | None = None) -> CurrentUser:
    cfg = _settings(settings)
    _assert_secret(cfg)
    try:
        header_b64, payload_b64, sig_b64 = token.split(".")
    except ValueError as exc:
        raise AuthError("malformed token") from exc

    signing_input = f"{header_b64}.{payload_b64}"
    expected = hmac.new(
        cfg.auth_secret_key.encode("utf-8"),
        signing_input.encode("ascii"),
        hashlib.sha256,
    ).digest()
    try:
        provided = _b64d(sig_b64)
    except Exception as exc:
        raise AuthError("invalid token signature") from exc
    if not hmac.compare_digest(expected, provided):
        raise AuthError("invalid token signature")

    try:
        header = json.loads(_b64d(header_b64))
        payload = TokenPayload.model_validate_json(_b64d(payload_b64))
    except (json.JSONDecodeError, ValidationError, ValueError) as exc:
        raise AuthError("invalid token payload") from exc
    if header.get("alg") != "HS256" or header.get("typ") != "RLT":
        raise AuthError("invalid token header")
    now = int(time.time())
    if payload.exp <= now:
        raise AuthError("token expired")
    if payload.iss != cfg.auth_issuer or payload.aud != cfg.auth_audience:
        raise AuthError("token audience mismatch")
    return CurrentUser(user_id=payload.sub, tenant_id=payload.tenant_id, role=payload.role)
```

- [ ] **Step 4: 检查强类型约束**

确认 `src/reflexlearn/common/auth.py` 没有引入 `Any`，没有保留 `type: ignore`，公共函数参数使用 `Settings | None`、`CurrentUser`、`TokenPayload` 等强类型。

- [ ] **Step 5: 运行测试验证通过**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：Task 1 和 Task 2 的测试全部通过。

---

## 6. Task 3：实现 FastAPI 鉴权依赖和 auth 路由

**Files:**

- Create: `src/reflexlearn/api/deps.py`
- Create: `src/reflexlearn/api/routes/auth.py`
- Modify: `src/reflexlearn/api/app.py`
- Test: `tests/unit/test_auth_security.py`

- [ ] **Step 1: 追加失败测试**

在 `tests/unit/test_auth_security.py` 追加：

```python
from fastapi.testclient import TestClient

from reflexlearn.api.app import create_app


def _login_headers(client: TestClient) -> dict[str, str]:
    resp = client.post(
        "/api/auth/login",
        json={"username": "admin", "password": "reflexlearn-admin"},
    )
    assert resp.status_code == 200
    token = resp.json()["access_token"]
    return {"Authorization": f"Bearer {token}"}


def test_login_and_me_route():
    client = TestClient(create_app())
    headers = _login_headers(client)
    me = client.get("/api/auth/me", headers=headers)
    assert me.status_code == 200
    assert me.json()["user_id"] == "admin"
    assert me.json()["tenant_id"] == "default"
    assert me.json()["role"] == "admin"


def test_me_requires_auth():
    client = TestClient(create_app())
    resp = client.get("/api/auth/me")
    assert resp.status_code == 401
    assert resp.json()["detail"] == "authentication_required"


def test_login_rejects_bad_password():
    client = TestClient(create_app())
    resp = client.post("/api/auth/login", json={"username": "admin", "password": "bad"})
    assert resp.status_code == 401
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：失败，`/api/auth/login` 和 `/api/auth/me` 不存在。

- [ ] **Step 3: 新建 `src/reflexlearn/api/deps.py`**

实现：

```python
from __future__ import annotations

from collections.abc import Callable

from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer

from reflexlearn.common.auth import AuthError, CurrentUser, verify_token
from reflexlearn.common.config import get_settings

_bearer = HTTPBearer(auto_error=False)


async def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(_bearer),
) -> CurrentUser:
    settings = get_settings()
    if not settings.auth_enabled:
        return CurrentUser(
            user_id=settings.auth_demo_username,
            tenant_id=settings.auth_demo_tenant_id,
            role="admin",
        )
    if credentials is None or credentials.scheme.lower() != "bearer":
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="authentication_required",
        )
    try:
        return verify_token(credentials.credentials, settings)
    except AuthError as exc:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="authentication_required",
        ) from exc


def require_roles(*roles: str) -> Callable[[CurrentUser], CurrentUser]:
    async def _dependency(user: CurrentUser = Depends(get_current_user)) -> CurrentUser:
        if user.role not in roles:
            raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="permission_denied")
        return user

    return _dependency
```

- [ ] **Step 4: 新建 `src/reflexlearn/api/routes/auth.py`**

实现：

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, status
from pydantic import BaseModel

from reflexlearn.api.deps import get_current_user
from reflexlearn.common.auth import AuthError, AuthToken, CurrentUser, authenticate_demo_user, issue_token
from reflexlearn.common.config import get_settings

router = APIRouter()


class LoginRequest(BaseModel):
    username: str
    password: str


@router.post("/auth/login")
async def login(body: LoginRequest) -> AuthToken:
    settings = get_settings()
    try:
        user = authenticate_demo_user(body.username, body.password, settings)
        token = issue_token(user, settings)
    except AuthError as exc:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="invalid_credentials",
        ) from exc
    return AuthToken(
        access_token=token,
        expires_in=settings.auth_token_ttl_seconds,
        user=user,
    )


@router.get("/auth/me")
async def me(user: CurrentUser = Depends(get_current_user)) -> CurrentUser:
    return user
```

- [ ] **Step 5: 修改 `src/reflexlearn/api/app.py` 注册 auth router**

把 import 改为：

```python
from reflexlearn.api.routes import auth, chat, health, knowledge, video
```

在 health 后加入：

```python
    app.include_router(auth.router, prefix="/api", tags=["auth"])
```

- [ ] **Step 6: 运行测试验证通过**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：auth 相关测试通过。

---

## 7. Task 4：保护 chat / knowledge / video 路由并注入可信用户

**Files:**

- Modify: `src/reflexlearn/api/routes/chat.py`
- Modify: `src/reflexlearn/api/routes/knowledge.py`
- Modify: `src/reflexlearn/api/routes/video.py`
- Modify: `tests/unit/test_knowledge_upload.py`
- Modify: `tests/unit/test_kafka_io.py`
- Modify: `tests/unit/test_video_jobs.py`
- Test: `tests/unit/test_auth_security.py`

- [ ] **Step 1: 追加失败测试**

在 `tests/unit/test_auth_security.py` 追加：

```python
def test_chat_requires_auth():
    client = TestClient(create_app())
    resp = client.post("/api/chat", json={"message": "线性回归", "user_id": "attacker"})
    assert resp.status_code == 401


def test_upload_requires_auth():
    client = TestClient(create_app())
    resp = client.post(
        "/api/knowledge/upload",
        files={"file": ("note.txt", b"hello", "text/plain")},
    )
    assert resp.status_code == 401


def test_video_submit_requires_auth():
    client = TestClient(create_app())
    resp = client.post("/api/video/jobs", json={"storyboard": "scene"})
    assert resp.status_code == 401
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：失败，因为现有接口未要求鉴权。

- [ ] **Step 3: 修改 `src/reflexlearn/api/routes/chat.py`**

新增 import：

```python
from fastapi import APIRouter, Depends
from reflexlearn.api.deps import get_current_user
from reflexlearn.common.auth import CurrentUser
```

保留 `ChatRequest.user_id` 字段兼容旧前端，但 route 不再信任它：

```python
@router.post("/chat")
async def chat(req: ChatRequest, user: CurrentUser = Depends(get_current_user)):
    session_id = req.session_id or uuid.uuid4().hex
    return StreamingResponse(
        event_stream(req.message, user.user_id, session_id),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "X-Accel-Buffering": "no",
            "Content-Type": "text/event-stream; charset=utf-8",
        },
    )
```

- [ ] **Step 4: 修改 `src/reflexlearn/api/routes/knowledge.py`**

新增 import：

```python
from fastapi import APIRouter, Depends, File, Form, UploadFile
from reflexlearn.api.deps import get_current_user
from reflexlearn.common.auth import CurrentUser
```

route 签名改为：

```python
async def upload_knowledge(
    file: UploadFile = File(...),
    course_id: str = Form(""),
    visibility: str = Form("public"),
    title: str = Form(""),
    enable_contextual: bool = Form(False),
    enable_graph_build: bool = Form(False),
    user: CurrentUser = Depends(get_current_user),
):
```

调用 `enqueue_document` 和 `ingest_document` 时使用：

```python
user_id=user.user_id
tenant_id=user.tenant_id
```

不要继续从表单接收可信 `user_id` / `tenant_id`。

- [ ] **Step 5: 修改 `src/reflexlearn/api/routes/video.py`**

新增 import：

```python
from fastapi import APIRouter, Depends
from reflexlearn.api.deps import get_current_user
from reflexlearn.common.auth import CurrentUser
```

两个 route 加依赖：

```python
@router.post("/video/jobs")
async def create_video_job(
    body: VideoJobRequest,
    user: CurrentUser = Depends(get_current_user),
):
    job = await submit_video_job(storyboard=body.storyboard, prompt=body.prompt)
    return {"job_id": job.job_id, "status": job.status}


@router.get("/video/jobs/{job_id}")
async def query_video_job(
    job_id: str,
    user: CurrentUser = Depends(get_current_user),
):
    job = await get_video_job(job_id)
    if job is None:
        return JSONResponse(status_code=404, content={"error": "job_not_found"})
    return job.model_dump()
```

本阶段只是“必须登录”，还没有把视频 job 绑定到 user。执行者不要扩大到 JobStore schema 改造。

- [ ] **Step 6: 更新现有 API 测试 helper**

在 `tests/unit/test_knowledge_upload.py` 增加：

```python
def auth_headers(client: TestClient) -> dict[str, str]:
    resp = client.post(
        "/api/auth/login",
        json={"username": "admin", "password": "reflexlearn-admin"},
    )
    assert resp.status_code == 200
    return {"Authorization": f"Bearer {resp.json()['access_token']}"}
```

所有 `/api/knowledge/upload` 请求都加：

```python
headers=auth_headers(client)
```

并把 `test_upload_passes_params` 的断言改为：

```python
    assert captured["user_id"] == "admin"
    assert captured["tenant_id"] == "default"
    assert captured["visibility"] == "private"
```

同时在请求 data 中保留伪造字段，证明后端忽略：

```python
            "user_id": "attacker",
            "tenant_id": "evil",
```

- [ ] **Step 7: 更新 Kafka route 测试**

在 `tests/unit/test_kafka_io.py` 增加同名 `auth_headers` helper，两个 route 请求加 `headers=auth_headers(client)`。

注意：不要让 helper 放到全局测试工具文件中，避免本轮扩大测试目录结构。

- [ ] **Step 8: 更新 video route 测试**

在 `tests/unit/test_video_jobs.py` 增加同名 `auth_headers` helper，`test_video_route_submit_and_query` 和 `test_video_route_404` 加 header。

`test_video_route_404` 改为：

```python
def test_video_route_404():
    client = TestClient(create_app())
    resp = client.get("/api/video/jobs/nonexistent-id", headers=auth_headers(client))
    assert resp.status_code == 404
```

- [ ] **Step 9: 运行定向测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py tests/unit/test_knowledge_upload.py tests/unit/test_kafka_io.py tests/unit/test_video_jobs.py -q
```

预期：相关测试通过。

---

## 8. Task 5：收紧 CORS 和 TrustedHost

**Files:**

- Modify: `src/reflexlearn/api/app.py`
- Test: `tests/unit/test_auth_security.py`

- [ ] **Step 1: 追加失败测试**

在 `tests/unit/test_auth_security.py` 追加：

```python
def test_cors_allows_configured_localhost_origin():
    client = TestClient(create_app())
    resp = client.options(
        "/api/auth/me",
        headers={
            "Origin": "http://localhost:3000",
            "Access-Control-Request-Method": "GET",
        },
    )
    assert resp.status_code == 200
    assert resp.headers.get("access-control-allow-origin") == "http://localhost:3000"


def test_cors_rejects_unconfigured_origin():
    client = TestClient(create_app())
    resp = client.options(
        "/api/auth/me",
        headers={
            "Origin": "http://evil.example",
            "Access-Control-Request-Method": "GET",
        },
    )
    assert resp.headers.get("access-control-allow-origin") is None
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：第二个测试失败，因为当前 `allow_origins=["*"]`。

- [ ] **Step 3: 修改 `src/reflexlearn/api/app.py`**

新增 import：

```python
from fastapi.middleware.trustedhost import TrustedHostMiddleware
from reflexlearn.common.config import get_settings
```

新增 helper：

```python
def _split_csv(value: str) -> list[str]:
    return [item.strip() for item in value.split(",") if item.strip()]
```

在 `create_app()` 中：

```python
    settings = get_settings()
    app.add_middleware(
        TrustedHostMiddleware,
        allowed_hosts=_split_csv(settings.trusted_hosts),
    )
    app.add_middleware(
        CORSMiddleware,
        allow_origins=_split_csv(settings.cors_allow_origins),
        allow_credentials=True,
        allow_methods=["GET", "POST", "OPTIONS"],
        allow_headers=["Authorization", "Content-Type"],
    )
```

删除原来的：

```python
        allow_origins=["*"],
        allow_methods=["*"],
        allow_headers=["*"],
```

- [ ] **Step 4: 运行定向测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：CORS 测试通过。

---

## 9. Task 6：上传基础校验

**Files:**

- Create: `src/reflexlearn/api/upload_guard.py`
- Modify: `src/reflexlearn/api/routes/knowledge.py`
- Test: `tests/unit/test_auth_security.py`

- [ ] **Step 1: 追加失败测试**

在 `tests/unit/test_auth_security.py` 追加：

```python
def test_upload_rejects_unsupported_extension(monkeypatch):
    import reflexlearn.common.db as db

    async def _no_pg():
        return None

    monkeypatch.setattr(db, "get_pg_pool", _no_pg)
    client = TestClient(create_app())
    resp = client.post(
        "/api/knowledge/upload",
        files={"file": ("shell.exe", b"MZ\x00\x00", "application/octet-stream")},
        headers=_login_headers(client),
    )
    assert resp.status_code == 415
    assert resp.json()["detail"] == "unsupported_file_type"


def test_upload_rejects_mime_mismatch(monkeypatch):
    import reflexlearn.common.db as db

    async def _no_pg():
        return None

    monkeypatch.setattr(db, "get_pg_pool", _no_pg)
    client = TestClient(create_app())
    resp = client.post(
        "/api/knowledge/upload",
        files={"file": ("note.md", b"%PDF- fake", "application/pdf")},
        headers=_login_headers(client),
    )
    assert resp.status_code == 415
    assert resp.json()["detail"] == "file_content_mismatch"


def test_upload_rejects_bad_visibility(monkeypatch):
    import reflexlearn.common.db as db

    async def _no_pg():
        return None

    monkeypatch.setattr(db, "get_pg_pool", _no_pg)
    client = TestClient(create_app())
    resp = client.post(
        "/api/knowledge/upload",
        files={"file": ("note.md", b"# T\n\nbody", "text/markdown")},
        data={"visibility": "world"},
        headers=_login_headers(client),
    )
    assert resp.status_code == 400
    assert resp.json()["detail"] == "invalid_visibility"
```

- [ ] **Step 2: 运行失败测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py -q
```

预期：失败，因为上传校验尚未实现。

- [ ] **Step 3: 新建 `src/reflexlearn/api/upload_guard.py`**

实现：

```python
from __future__ import annotations

from pathlib import PurePath

from fastapi import HTTPException, UploadFile, status
from pydantic import BaseModel

from reflexlearn.common.config import Settings, get_settings

_TEXT_EXTS = {"txt", "md", "html", "htm"}
_ZIP_EXTS = {"docx", "pptx"}
_VISIBILITY = {"public", "tenant", "private"}
_EXT_MIME = {
    "pdf": {"application/pdf"},
    "docx": {"application/vnd.openxmlformats-officedocument.wordprocessingml.document"},
    "pptx": {"application/vnd.openxmlformats-officedocument.presentationml.presentation"},
    "html": {"text/html"},
    "htm": {"text/html"},
    "md": {"text/markdown", "text/plain"},
    "txt": {"text/plain"},
}


class ValidatedUpload(BaseModel):
    filename: str
    raw: bytes
    extension: str
    content_type: str


def validate_visibility(value: str) -> str:
    if value not in _VISIBILITY:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="invalid_visibility")
    return value


def _split_csv(value: str) -> set[str]:
    return {item.strip().lower() for item in value.split(",") if item.strip()}


def _safe_filename(filename: str | None) -> str:
    name = PurePath((filename or "upload.bin").replace("\\", "/")).name
    return name or "upload.bin"


def _extension(filename: str) -> str:
    if "." not in filename:
        return ""
    return filename.rsplit(".", 1)[-1].lower()


def _looks_like_text(raw: bytes) -> bool:
    if b"\x00" in raw[:4096]:
        return False
    try:
        raw[:4096].decode("utf-8")
        return True
    except UnicodeDecodeError:
        return False


def _validate_magic(ext: str, raw: bytes) -> None:
    if ext == "pdf" and not raw.startswith(b"%PDF-"):
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="file_content_mismatch")
    if ext in _ZIP_EXTS and not raw.startswith(b"PK\x03\x04"):
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="file_content_mismatch")
    if ext in _TEXT_EXTS and not _looks_like_text(raw):
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="file_content_mismatch")
    if ext == "md" and raw.startswith(b"%PDF-"):
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="file_content_mismatch")


def _validate_mime_for_ext(ext: str, content_type: str) -> None:
    if not content_type:
        return
    if content_type not in _EXT_MIME.get(ext, set()):
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="file_content_mismatch")


async def read_validated_upload(
    file: UploadFile,
    settings: Settings | None = None,
) -> ValidatedUpload:
    cfg = settings or get_settings()
    filename = _safe_filename(file.filename)
    ext = _extension(filename)
    allowed_exts = _split_csv(cfg.allowed_upload_extensions)
    allowed_mimes = _split_csv(cfg.allowed_upload_mime_types)
    content_type = (file.content_type or "").split(";")[0].lower()

    if ext not in allowed_exts:
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="unsupported_file_type")
    if content_type and content_type not in allowed_mimes:
        raise HTTPException(status_code=status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="unsupported_file_type")
    _validate_mime_for_ext(ext, content_type)

    raw = bytearray()
    while True:
        chunk = await file.read(1024 * 1024)
        if not chunk:
            break
        raw.extend(chunk)
        if len(raw) > cfg.max_upload_bytes:
            raise HTTPException(status_code=status.HTTP_413_REQUEST_ENTITY_TOO_LARGE, detail="upload_too_large")

    data = bytes(raw)
    if not data:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="empty_upload")
    _validate_magic(ext, data)
    return ValidatedUpload(filename=filename, raw=data, extension=ext, content_type=content_type)
```

- [ ] **Step 4: 修改 `src/reflexlearn/api/routes/knowledge.py`**

新增 import：

```python
from reflexlearn.api.upload_guard import read_validated_upload, validate_visibility
```

替换：

```python
    raw = await file.read()
    filename = file.filename or "upload.bin"
```

为：

```python
    settings = get_settings()
    visibility = validate_visibility(visibility)
    upload = await read_validated_upload(file, settings)
    raw = upload.raw
    filename = upload.filename
```

注意：如果文件里已有 `settings = get_settings()`，不要重复定义。

- [ ] **Step 5: 运行定向测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py tests/unit/test_knowledge_upload.py -q
```

预期：上传安全测试和原上传契约测试通过。

---

## 10. Task 7：更新活体脚本 `scripts/check_api.sh`

**Files:**

- Modify: `scripts/check_api.sh`

- [ ] **Step 1: 修改脚本内 Python，增加登录函数**

在 `API = ...` 后加入：

```python
AUTH_USER = os.getenv("REFLEXLEARN_HEALTH_USER", "admin")
AUTH_PASSWORD = os.getenv("REFLEXLEARN_HEALTH_PASSWORD", "reflexlearn-admin")
```

新增：

```python
async def login_headers(client: httpx.AsyncClient) -> dict[str, str]:
    resp = await client.post(
        f"{API}/auth/login",
        json={"username": AUTH_USER, "password": AUTH_PASSWORD},
    )
    resp.raise_for_status()
    token = resp.json()["access_token"]
    print("[OK] api auth login")
    return {"Authorization": f"Bearer {token}"}
```

- [ ] **Step 2: 上传和视频请求携带 header**

`check_upload` 签名改为：

```python
async def check_upload(client: httpx.AsyncClient, headers: dict[str, str]) -> None:
```

上传 data 删除：

```python
        "user_id": "health-user",
        "tenant_id": "default",
```

上传请求改为：

```python
    resp = await client.post(
        f"{API}/knowledge/upload",
        files=files,
        data=data,
        headers=headers,
        timeout=180,
    )
```

`check_video` 签名改为：

```python
async def check_video(client: httpx.AsyncClient, headers: dict[str, str]) -> None:
```

提交和查询都加 `headers=headers`。

`main()` 改为：

```python
async def main() -> None:
    async with httpx.AsyncClient(timeout=30) as client:
        await wait_api(client)
        headers = await login_headers(client)
        await check_upload(client, headers)
        await check_video(client, headers)
```

- [ ] **Step 3: 校验 shell 语法**

运行：

```bash
bash -n scripts/*.sh
```

预期：无输出，退出码 0。

---

## 11. Task 8：全量验证并修复回归

**Files:**

- Potentially modify: files touched above

- [ ] **Step 1: 运行 P0 定向测试**

运行：

```bash
bash scripts/test_unit.sh tests/unit/test_auth_security.py tests/unit/test_knowledge_upload.py tests/unit/test_kafka_io.py tests/unit/test_video_jobs.py -q
```

预期：全部通过。

- [ ] **Step 2: 运行全量单测**

运行：

```bash
bash scripts/test_unit.sh
```

预期：通过。基线是 `292 passed, 2 warnings`，本轮新增测试后通过数量会增加。

- [ ] **Step 3: 检查 shell 脚本语法**

运行：

```bash
bash -n scripts/*.sh
```

预期：无输出，退出码 0。

- [ ] **Step 4: 可选活体**

只有在当前机器服务可启动时执行：

```bash
bash scripts/start_api.sh 8001
bash scripts/check_api.sh 8001
bash scripts/stop_api.sh 8001
```

预期：

- `[OK] api health`
- `[OK] api auth login`
- `[OK] knowledge upload ...`
- `[OK] video job degraded honestly ...`

如果 8001 被占用，换 8002；仍然必须使用 `scripts/*.sh`。

---

## 12. Task 9：更新接管文档

**Files:**

- Modify: `PROGRESS.md`

- [ ] **Step 1: 更新顶部“最后更新”**

追加本轮成果：

```markdown
P0 最小安全闭环第一包完成：新增后端演示登录、HMAC token、CurrentUser 鉴权依赖；chat/knowledge/video API 默认要求 Bearer token；knowledge 上传不再信任表单 user_id/tenant_id；CORS 改为配置化白名单；上传增加大小、扩展名、MIME、魔数和 visibility 基础校验；check_api 活体脚本支持登录后校验。
```

- [ ] **Step 2: 在第 2.2 常用命令补充登录说明**

在 API 检查附近加入：

```bash
# P0 安全底座后，check_api 会自动用 REFLEXLEARN_HEALTH_USER / REFLEXLEARN_HEALTH_PASSWORD 登录。
# 默认开发账号：admin / reflexlearn-admin；生产必须通过环境变量改掉。
bash scripts/check_api.sh 8001
```

- [ ] **Step 3: 在“诚实记录”或本轮记录中说明限制**

加入：

```markdown
诚实限制：当前只是最小后端安全边界，仍未完成数据库用户体系、前端登录页、上传隔离区、病毒扫描、内容审核、防盗链签名 URL、AI Safety Gateway、视频作业对象级归属校验。
```

---

## 13. 提交前自查清单

执行者完成后必须逐项检查：

- [ ] 没有把 token、密码、secret 打印到日志。
- [ ] 生产环境默认 secret 会被拒绝。
- [ ] `src/reflexlearn/api/app.py` 不再出现 `allow_origins=["*"]`。
- [ ] `/api/health` 仍然公开可访问。
- [ ] `/api/chat` 未登录返回 401。
- [ ] `/api/knowledge/upload` 未登录返回 401。
- [ ] `/api/video/jobs` 未登录返回 401。
- [ ] 上传 route 不再从表单读取可信 `user_id` / `tenant_id`。
- [ ] 非法扩展名返回 415。
- [ ] visibility 非法返回 400。
- [ ] 所有新增 Python 文件尽量控制在 300 行内。
- [ ] 没有新增 `Any` 或未结构化 `dict` 作为核心数据结构。
- [ ] 所有验证命令走 `scripts/*.sh`。

---

## 14. 回报模板

新窗口执行完成后，请按这个格式回报，便于主窗口 review：

```markdown
## P0 最小安全闭环执行回报

### 1. 完成范围
- 完成了哪些 Task：
- 未完成哪些 Task：

### 2. 修改文件
- 新建：
- 修改：

### 3. 关键实现
- token 方案：
- 路由保护：
- 租户注入：
- CORS：
- 上传校验：

### 4. 验证结果
- `bash scripts/test_unit.sh tests/unit/test_auth_security.py tests/unit/test_knowledge_upload.py tests/unit/test_kafka_io.py tests/unit/test_video_jobs.py -q`：
- `bash scripts/test_unit.sh`：
- `bash -n scripts/*.sh`：
- `bash scripts/check_api.sh <port>`：

### 5. 已知限制
- 

### 6. 需要 review 的点
- 
```

---

## 15. Review 重点

主窗口后续 review 时优先看：

1. 鉴权是否真的保护了 route，而不是只加了登录接口。
2. `tenant_id` 是否仍可能从用户表单伪造。
3. CORS 是否真的移除了 `* + credentials`。
4. 上传是否仍有直接 `file.read()` 绕过 `upload_guard`。
5. 现有 M4/M5 单测是否被跳过或弱化。
6. 脚本是否仍遵守 `scripts/*.sh` 启停和验证约束。
